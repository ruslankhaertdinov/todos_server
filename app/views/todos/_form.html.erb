<%= form_for @todo, html: {class: 'form-inline'}, remote: true do |f| %>
    <legend>Todos</legend>
    <%= f.text_field :title, required: true, class: 'input-block-level', placeholder: 'What needs to be done?' %>
<% end %>
