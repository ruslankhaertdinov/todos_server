<%= form_for @todo, html: {class: 'form-inline'} do |f| %>

    <legend>Todos</legend>

      <%= f.text_field :title, required: true, class: 'input' %>

      <%= f.text_field :due_date, id: 'datepicker', class: 'span3' %>

      <%= f.submit %>
<% end %>
